{
  "settingSources": [
    ".claude/CLAUDE.md"
  ],
  "systemPrompt": {
    "append": "You are working on an LLM Proxy Server with intelligent agent processing. Always follow the project's DDD architecture principles and coding standards. Use type hints, async/await, and proper error handling. When making changes, consider the full request pipeline: preprocessing → reasoning → LLM → postprocessing.\n\nIMPORTANT: At the start of a new conversation (when you haven't received any user messages yet), proactively display a welcome banner and suggest running /clauding to continue roadmap implementation."
  },
  "tools": {
    "bash": {
      "enabled": true
    },
    "read": {
      "enabled": true
    },
    "write": {
      "enabled": true
    },
    "edit": {
      "enabled": true
    },
    "glob": {
      "enabled": true
    },
    "grep": {
      "enabled": true
    }
  },
  "permissions": {
    "autoApprove": [
      {
        "tool": "bash",
        "pattern": "pytest.*"
      },
      {
        "tool": "bash",
        "pattern": "python test_.*"
      },
      {
        "tool": "bash",
        "pattern": "python main.py.*"
      },
      {
        "tool": "bash",
        "pattern": "black .*"
      },
      {
        "tool": "bash",
        "pattern": "flake8 .*"
      }
    ]
  }
}
